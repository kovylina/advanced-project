language: node_js
node_js:
  - "8.11.2"
cache:
  directories:
  - node_modules
script:
  - npm run build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $advanced
  local-dir: dist
  target-branch: gh-pages
  on:
      branch: dev